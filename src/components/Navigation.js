export function Navigation() {
    return (
        <nav className="navbar navbar-dark bg-dark">
          <div className="container-fluid">
            <a href="eslint-disable-next-line" className="navbar-brand mb-0 h1">{this.props.titulo}</a>
          </div>
        </nav>
    );
  }